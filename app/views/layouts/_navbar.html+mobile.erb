<nav class="bg-white shadow-md w-full">
  <div class="container mx-auto px-4">
    <div class="flex justify-between items-center">
      <% unless current_user %>
        <div class="flex justify-between w-full">
          <div>MoneyApp</div>
        </div>
      <% else %>
        <div class="flex items-center justify-between w-full">
          <button id="sidebar-toggle" class="p-2 rounded-md hover:bg-gray-100 focus:outline-none lg:hidden">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
            </svg>
          </button>
          <div>
            <%= link_to "Add Transaction", new_trx_path, class: "p-3 bg-blue-500 hover:bg-blue-600 text-white rounded-md whitespace-nowrap" %>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</nav>

<% if current_user %>
  <%= render 'layouts/sidebar' %>
<% end %>
